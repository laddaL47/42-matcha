import { Container, Text, Title } from "@mantine/core";

const Settings = () => {
  return (
    <Container>
      <Title order={2}>Settings</Title>
      <Text c="dimmed">Configure your account preferences.</Text>
    </Container>
  );
};

export default Settings;
